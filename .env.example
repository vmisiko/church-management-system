# Points the frontend at the backend API. Without this file the app falls back to
# http://localhost:3001 (core/utility/NetworkConstants.ts) — which is not the backend
# on this machine, so every API call fails silently until you create .env.local.
NEXT_PUBLIC_API_URL=http://localhost:3005
